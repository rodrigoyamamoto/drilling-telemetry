<Project>

    <PropertyGroup>
        <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    </PropertyGroup>

    <!-- ASP.NET Core / API: OpenAPI document generation and API documentation UI (Scalar) -->
    <ItemGroup>
        <PackageVersion Include="Microsoft.AspNetCore.OpenApi" Version="10.0.11" />
        <PackageVersion Include="Scalar.AspNetCore" Version="2.16.20" />
    </ItemGroup>

    <!-- Hosting / DI / Validation: Generic Host, dependency injection, configuration and model validation -->
    <ItemGroup>
        <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.10" />
        <PackageVersion Include="Microsoft.Extensions.Validation" Version="10.0.10" />
    </ItemGroup>

    <!-- Configuration: Local development environment file loading -->
    <ItemGroup>
        <PackageVersion Include="DotNetEnv" Version="3.2.0" />
    </ItemGroup>

    <!-- Messaging: RabbitMQ client to publish and consume messages via AMQP -->
    <ItemGroup>
        <PackageVersion Include="RabbitMQ.Client" Version="7.2.2" />
    </ItemGroup>

    <!-- Persistence: lightweight SQL mapping, PostgreSQL data provider and connection pooling -->
    <ItemGroup>
        <PackageVersion Include="Dapper" Version="2.1.79" />
        <PackageVersion Include="Npgsql" Version="10.0.3" />
    </ItemGroup>

    <!-- Testing: Test SDK, xUnit framework, Test Explorer adapter and FakeTimeProvider -->
    <ItemGroup>
        <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
        <PackageVersion Include="xunit" Version="2.9.3" />
        <PackageVersion Include="xunit.runner.visualstudio" Version="4.0.0" />
        <PackageVersion Include="Microsoft.Extensions.TimeProvider.Testing" Version="10.9.0" />
    </ItemGroup>

</Project>
